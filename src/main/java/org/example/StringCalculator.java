package org.example;

public class StringCalculator {
    public static int add(String numbers) {
        int sum = 0;
        if (numbers == null || numbers.isEmpty()) {
            return 0;
        }
        return sum;
    }
}
